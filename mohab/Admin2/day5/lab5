1-sudo useradd user1
  sudo useradd user2
  sudo useradd user3
  sudo useradd user4  
  sudo useradd user5
  sudo useradd user6
  sudo useradd user7
  sudo passwd user1
  sudo passwd user2
  sudo passwd user3
  sudo passwd user4
  sudo passwd user5
  sudo passwd user6
  sudo passwd user7

2- sudo groupadd -g 10000 sales
sudo groupadd -g 10001 hr
sudo groupadd -g 10002 web
because I want to diffrentiate between these groubs and the primary groups for users

3-
sudo usermod -a -G sales user1
usermod -a -G sales user2
sudo usermod -a -G hr user3
sudo usermod -a -G hr user4
sudo usermod -a -G web user5
sudo usermod -a -G web user6
usermod -a -G web user7
usermod -a -G hr user7
sudo usermod -a -G sales user7

4- su - user1
id
=>with cat /etc/group

5- 
mkdir dept
sudo mkdir web
sudo mkdir hr
sudo mkdir sales

6-
sudo chgrp hr hr
sudo chgrp sales sales
sudo chgrp web web

7-
sudo chmod 755 depts
cd depts
sudo chmod 770 *

8-
chmod g+s . for hr
chmod g+s . for sales
chmod g+s . for web

9-
su - user2
touch /depts/sales/user2.txt => created
touch /depts/hr/ user2.txt =>permission denied
touch /depts/web/ user2.txt =>permission denied

10-
User_Alias MOUNTUSERS= user3 , user4
Cmnd_Alias MOUNT=/sbin/mount,/sbin/umount
MOUNTUSERS ALL=MOUNT
user5 ALL=NOPASSWD: /sbin/fdisk

11-
su - user3
sudo umout /boot

12-
su - user4
mount /dev/sdb7 /boot
su - user5
fdisk -l

13-
su -
mkdir dir
chmod 770 dir
chown :sales dir
chmod g-w dir
14-
mkdir dir/dir2
chown :sales dir2

15-
chmod g+t .


//lsblk


