1. Find out what device node the /boot partition is

df -l
output:
Filesystem          1K-blocks     Used Available Use% Mounted on
/dev/sda4             1998672   283848   1593584  16% /boot
then:
cat /proc/partitions
output:
major minor  #blocks  name
8        4    2097152 sda4

or 
lsblk

2. Estimate the size in bytes of a level-zero dump for /boot

df -H
output:
Filesystem           Size  Used Avail Use% Mounted on
/dev/sda4            2.0G  278M  1.6G  16% /boot

so level 0 dump will be 2 * 10e9 bytes

3. Back up the data of /boot to a dump file in /var/tmp/dumpfile

<Note that I first installed dump package>
dump -0u -f /var/tmp/dumpfile /boot

4. Look in the /etc/dumpdates file and see how the dump command recorded the timestamp of the full backup

cat /etc/dumpdates
output:
/dev/sda4 0 Tue Feb  9 16:10:07 2021 +0200

5. Use the restore command to view the contents of the dump file

restore -tf /var/tmp/dumpfile
sample output:
--------------
Dump   date: Tue Feb  9 16:10:07 2021
Dumped from: the epoch
Level 0 dump of /boot on localhost.localdomain:/dev/sda4
Label: none
         2	.
        11	./lost+found
        12	./efi
      8193	./grub2
        74	./grub2/grubenv
      8194	./extlinux
        14	./extlinux/chain.c32
        13	./extlinux/cat.c32
        16	./extlinux/cmenu.c32
        15	./extlinux/cmd.c32


6. Use restore command in the interactive mode to extract /grub/splash.xpm.gz and /grub/grub.conf

note that (/grub/splash.xpm.gz and /grub/grub.conf don't exist)

restore -if /var/tmp/dumpfile
restore> add /grub/splash.xpm.gz
restore> add /grub/grub.conf
restore> extract
restore> 1
restore> y

------------------------------------------------------------------------------------------
7. Use fdisk -l to locate information about the partition sizes.
8. Use fdisk to add a new logical partition that is 1GB in size.
9. Did the kernel feel the changes? Display the content of /proc/partitions file? What did you
notice? How to overcome that?
10. Make a new ext2 file system on the new logical partition you just created.
11. Create a directory, name it /data.
12. Add a label to the new filesystem, name it data.
13. Add a new entry to /etc/fstab for the new filesystem using the label you just create.
14. Mount the new filesystem.
15. Display your swap size.
16. Create a swap file of size 512MB.
17. Add the swap file to the virtual memory of the system.
18. Display the swap size
------------------------------------------------------------------------------------------
19. Implement disk quotas for users on the /home directory by taking the following actions
a. Edit /etc/fstab and add the usrquota option to the /home filesystem

vi /etc/fstab
/dev/mapper/cl-home     /home                   xfs     defaults,usrquota,grpquota        0 0

b. Remount the filesystem with the command mount -o remount /home

mount -o remount /home

c. Use the quotacheck command to create the quota-tracking file quotacheck /home

quotacheck –vmac

d. Use the quotaon command to enable quota tracking by the kernel quotaon /home

quotaon /home

